# Web: use /api/v1 when frontend and backend share the same origin.
VITE_API_BASE_URL=/api/v1

# Native Android/iOS: set this to your reachable backend URL.
# Example: https://api.example.com/api/v1
VITE_NATIVE_API_BASE_URL=/api/v1
